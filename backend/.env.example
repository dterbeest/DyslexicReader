# Frontend origin allowed by CORS (update for production)
FRONTEND_ORIGIN=http://localhost:5173

# Rate limiting (requests per IP)
RATE_LIMIT=10/hour

# File size limits (bytes)
MAX_IMAGE_BYTES=10485760
MAX_PDF_BYTES=26214400
